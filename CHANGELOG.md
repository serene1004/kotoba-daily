# 변경 기록

배포 버전별로 기능 목록만 나열하지 않고, 이전 버전에서 무엇이 달라졌는지와 그 배경을 기록합니다.

## [0.1.2] - 2026-09-22

### 학습 흐름 개선

- 정답을 문자열로 자동 판정하지 않고 사용자가 `정답이에요` 또는 `정답이 아니에요`를 선택하도록 변경
- 입력 전 `모르겠어요`를 선택하면 오답으로 기록하고 정답을 바로 확인할 수 있도록 개선
- 후리가나 표시 설정을 저장해 다음 단어와 새로고침 후에도 유지

### 복습 및 데이터 관리 개선

- bookmark 여부와 복습 일정을 분리하고, `nextReviewAt` 기준으로 복습 단어를 출제
- 같은 날 이미 본 단어가 다시 출제되지 않도록 개선
- bookmark에 단어 전체 대신 ID만 저장하고 기존 데이터 자동 마이그레이션 지원
- 날짜 변경을 감지해 자정 이후에도 새로운 학습 세트를 불러오도록 개선

### 화면 및 배포 개선

- 사전 뜻 영역을 넓히고 품사·사전 링크 배치를 개선
- GitHub Pages와 Docker 배포 환경의 Vite base 경로 분리
- 학습 상태와 로직을 `useStudyProgress` 훅으로 분리
- 후리가나를 외부 API 없이 단어 전체 reading으로 표시
- localStorage namespace/version 추가 및 기존 키 fallback 지원
- `pnpm check:data`로 단어 ID, 필수 필드, 예문 누락을 검사

## [0.1.1] - 2026-09-18

### 수정

- 배포 후 발견된 학습 버튼 색상 회귀 수정

## [0.1.0] - 2026-09-18

### 이번 변경의 방향

이전 버전은 N5 단어 파일을 API 또는 정적 데이터에서 가져와 단어를 랜덤하게 보여주는 수준이었습니다. 사용자는 단어를 보고 뜻을 입력할 수 있었지만, 데이터 자체는 단순한 단어 목록에 가까웠고 복습이나 사전 정보가 충분하지 않았습니다.

이번 변경에서는 단어 목록을 실제 학습용 단어장에 가까운 데이터로 발전시켰습니다.

```text
기존: N5 단어 데이터 → 랜덤 출제
현재: JLPT CSV → 데이터 파싱 → OpenAI enrichment → Schema 검증 → n5.json → 학습·복습
```

### 데이터 파이프라인 개선

- elzup JLPT CSV에서 N5 원본 데이터를 가져오는 파싱 단계 추가
- OpenAI Responses API를 이용해 한국어 뜻, 품사, 자연스러운 예문을 자동 보강
- JSON Schema 검증으로 데이터 누락·형식 오류·단어 수 불일치 방지
- 배치별 체크포인트를 저장해 API 오류나 중단 이후 이어서 실행 가능
- 결과를 앱이 직접 사용하는 `public/data/n5.json`으로 정규화
- 현재 718개 N5 단어를 실제 학습 데이터로 구성

### 학습 경험 개선

- 단어를 랜덤하게만 보여주던 방식에서 신규 단어와 복습 단어를 함께 출제하는 방식으로 변경
- 사용자의 입력을 사전 뜻과 비교해 정답 여부를 기록
- 표현 차이로 오답 처리될 수 있는 상황을 고려해 정답 판정 후 사용자가 사전 뜻을 직접 확인하는 피드백 흐름 추가
- 사전 뜻, 품사, 예문, Jisho 링크를 함께 제공
- 정답·오답 결과에 따라 다음 복습일을 계산
- 헷갈리는 단어를 단어장에 저장하고 다음 복습 대상에 포함

### 제품 완성도 개선

- 학습 진행률과 단어장 데이터를 LocalStorage에 안정적으로 저장
- 새로고침 후 학습 위치와 북마크 유지
- 날짜별 학습 세트와 학습 기록 분리
- 로딩·오류·빈 데이터 상태 추가
- 단어장과 학습 화면의 버튼, 품사 chip, 예문 영역 가독성 개선
- 데스크톱 및 모바일 화면 검증

### 개발 기록

- `App.tsx`에 모여 있던 LocalStorage·복습 계산 로직을 공용 유틸리티로 분리
- README를 실제 데이터 생성 및 enrichment 실행 흐름에 맞게 최신화
- `pnpm format:check`, `pnpm lint`, `pnpm build` 통과

## [0.0.0] - 2026-08-02

- GitHub Actions를 통한 GitHub Pages 초기 배포
- N5 단어를 랜덤으로 제공하는 기본 학습 화면 구성
- 후리가나 표시, 단어장 저장, 라이트·다크 모드 추가
